name: blerpc_central
description: blerpc Flutter test app — functional tests and benchmarks against nRF54L15 DK.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  blerpc_protocol:
    git:
      url: https://github.com/tdaira/blerpc-protocol-dart.git
      ref: main
  flutter_blue_plus: ^1.35.0
  protobuf: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
