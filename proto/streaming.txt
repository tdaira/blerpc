# Format: <command_name> <direction>
#   p2c = peripheral-to-central (server-streaming, uses streamReceive)
#   c2p = central-to-peripheral (client-streaming, uses streamSend)
counter_stream p2c
counter_upload c2p
